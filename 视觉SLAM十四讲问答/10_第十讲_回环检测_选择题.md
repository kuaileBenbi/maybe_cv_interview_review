# 第十讲 回环检测 — 选择题

**10.1** ⭐ 回环检测在 SLAM 中的核心作用是？

A. 提取特征点
B. 检测机器人回到之前的位置，提供跨时间约束以消除累积漂移
C. 估计相机位姿
D. 构建稠密地图

**10.2** ⭐⭐ 词袋模型（BoW）中，"单词"是指？

A. 自然语言文字
B. 描述子空间聚类后的类别中心
C. 图像像素
D. 特征点坐标

**10.3** ⭐⭐ 构建视觉字典时使用树状结构的主要优势是？

A. 存储更小
B. 查找速度快（O(logN)）
C. 精度更高
D. 不需要训练

**10.4** ⭐⭐ TF-IDF 中，IDF（逆文档频率）的含义是？

A. 单词在该图像中出现的频率
B. log(总图像数 / 包含该单词的图像数)，越常见的单词值越低
C. 单词的区分度，越常见越高
D. 图像的总体亮度

**10.5** ⭐⭐ 回环检测中为什么需要连续性检测？

A. 提高计算速度
B. 单帧匹配不可靠（场景相似导致误匹配），需要连续多帧都匹配才确认
C. 减少内存使用
D. 检测特征点

**10.6** ⭐⭐ 在 SLAM 回环检测中，应该优先保证哪个指标？

A. 召回率（Recall）
B. 精确率（Precision）—— 错误回环比漏检更危险
C. 运行速度
D. 内存使用

**10.7** ⭐⭐ 回环检测的几何验证通常使用什么方法过滤错误匹配？

A. 颜色匹配
B. 特征点匹配 + RANSAC 计算本质矩阵/基础矩阵
C. 模板匹配
D. 光流跟踪

**10.8** ⭐⭐ VLAD 相比 BoW 的改进是？

A. 计算更快
B. 保留了描述子与聚类中心的残差信息，而非仅计数
C. 不需要字典
D. 支持更多特征类型
